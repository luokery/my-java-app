<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.CustomerMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO customer (username, nickname, email, password, gender, birth_date, status, created_at)
        VALUES (#{username}, #{nickname}, #{email}, #{password}, #{gender}, #{birthDate}, #{status}, #{createdAt})
    </insert>

    <select id="findById" resultType="com.example.demo.model.entity.Customer">
        SELECT id, username, nickname, email, gender, birth_date, status, created_at
        FROM customer
        WHERE id = #{id}
    </select>

    <update id="update">
        UPDATE customer
        <set>
            <if test="username != null">username = #{username},</if>
            <if test="nickname != null">nickname = #{nickname},</if>
            <if test="email != null">email = #{email},</if>
            <if test="password != null">password = #{password},</if>
            <if test="gender != null">gender = #{gender},</if>
            <if test="birthDate != null">birth_date = #{birthDate},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id}
    </update>

    <select id="findAll" resultType="com.example.demo.model.entity.Customer">
        SELECT id, username, nickname, email, gender, birth_date, status, created_at
        FROM customer
    </select>

    <delete id="deleteById">
        DELETE FROM customer WHERE id = #{id}
    </delete>

</mapper>
